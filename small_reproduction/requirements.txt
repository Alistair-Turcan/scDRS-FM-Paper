# Python dependencies for the scDRS-FM 10k-scale reproduction.
# The scDRS-FM package itself is vendored unmodified under scDRS-FM-main/ and is
# installed separately (`pip install -e scDRS-FM-main` or via PYTHONPATH).
#
# ---- core scoring stack (matches scDRS-FM-main/requirements.txt) ----
numpy
pandas
scipy
scanpy
anndata
scdrs
statsmodels
magic-impute
scprep
#
# ---- clustering / graph (Leiden, used by scanpy neighbors in ct downstream) ----
leidenalg
python-igraph
#
# ---- enrichment / annotation used by notebooks 09 and 10 ----
gseapy==1.3.0
gprofiler-official
#
# ---- plotting (figures + notebooks) ----
matplotlib
seaborn
#
# ---- notebook execution ----
jupyter
nbconvert
ipykernel
#
# NOTE (non-pip dependency): MAGMA is a standalone C++ binary required only for
# regenerating the gene-level Z matrix (scripts/12_run_magma_geneanalysis.sh).
# Install it via download/download_magma_reference.sh (MAGMA v1.10 + 1000G EUR).
